# AI Bridge v0.4.4 Real Dogfood Retry

## Scope

This document records a real local Claude Code dogfood run driven through the
AI Bridge tools exposed to a fresh Codex thread. The run was deliberately
documentation-only. It did not change core code, scripts, tests, versions, CI
configuration, the plugin manifest, skills, tags, or releases.

This is validation evidence for the existing v0.4.3 implementation. The
v0.4.4 label names the validation gate; no package or plugin version was
bumped.

## Baseline

- Repository: `C:\Users\xsjhxs\Desktop\ai_bridge`
- Branch: `master`
- HEAD: `de2ba19096a8a3ccd0c6c4d94e8e68a75863a664`
- Commit: `Document final v0.4.3 validation`
- Package/app version: `0.4.3`
- Plugin version: `0.4.3+codex.20260628100000`
- Node: `v22.22.1`
- npm: `10.9.4`
- Claude Code: `2.1.105`

The 15 pre-existing untracked files under
`hatch-pet-runs/silver-armor-girl/` were user-owned. AI Bridge captured their
SHA-256 hashes during preflight, and the post-run snapshot reported no modified
pre-existing untracked files.

## Fresh Thread Tool Exposure

The current fresh Codex thread exposed the AI Bridge MCP namespace. Codex
discovered and called `ai_bridge_discover_workspace_runs` first, then used the
preflight, handoff, asynchronous Claude iteration, polling, workspace recovery,
Run Explorer, verification, review, export, and usage-summary tools.

This direct observation proves tool exposure in this thread only. AI Bridge was
already installed as an active personal plugin at
`0.4.3+codex.20260628100000`; it was not installed or updated during this run.
The observation does not prove that plugin discovery is fixed for every new
thread or Codex installation.

## Local Diagnostics

The following repository-root diagnostics were run before the real iteration:

- `npm run check`: passed.
- `npm run smoke:mcp-tools`: passed; server version `0.4.3`; 19 tools listed;
  every required AI Bridge tool was present.
- `npm run diagnose:plugin`: passed; repository and installed-plugin layout
  checks were internally consistent.
- `npm run diagnose:codex-discovery -- --json`: passed; 7 fixed path hints were
  checked; its local-script thread-exposure result remained `unknown`.

Direct fresh-thread observation, rather than the local discovery script,
established exposure for this run.

## Dogfood Decision

Real dogfood proceeded because the required tools were callable and local
diagnostics passed. Discovery found no active run suitable for reuse. The only
older exact-workspace candidate was an unrelated terminal failed run, so Codex
created one new run through preflight, prepared one bounded plan handoff, and
started the explicitly approved Claude iteration.

## Run IDs

- New dogfood run: `run-20260628052600-7ikdkf`
- Older unrelated failed run observed during discovery:
  `run-20260619055625-voo279`

No additional run was created during recovery or review.

## Task IDs

- Iteration 1: `task-20260628052634-xugsyp`

No second task or iteration was started.

## Claude Session

- Session ID: `9ae949e5-f8cd-4bd8-a6bb-4e5e94d9f288`
- Invocation mode: `session-id`
- Claude PID: `14944`
- Durable worker PID: `35540`
- Started: `2026-06-28T05:26:35.265Z`
- Finished: `2026-06-28T05:27:17.225Z`
- Exit code: `0`
- Timed out: `false`
- Finalization phase: `complete`

Attach and workspace poll both returned the same run, task, and Claude session
identifiers.

## Steps Performed

1. Ran the source, plugin, tool-registration, and Codex-discovery diagnostics.
2. Confirmed callable `ai_bridge_*` tools in the fresh Codex thread.
3. Called `ai_bridge_discover_workspace_runs` before preflight.
4. Created one run with `ai_bridge_preflight` and captured the dirty-worktree
   baseline.
5. Prepared plan handoff index 1, limiting Claude to this validation document.
6. Started iteration 1 with `ai_bridge_start_claude_iteration`.
7. Polled the task to terminal `completed` state with no corrupt transcript
   lines.
8. Rediscovered, attached, and polled the completed workspace run.
9. Captured a baseline-aware change snapshot.
10. Ran structured verification through AI Bridge.
11. Exercised all six Run Explorer interfaces and exported JSON and Markdown
    reports.
12. Reviewed Claude's output, replaced its evidence placeholders, corrected
    stale example tool names, and updated `CONTEXT_HANDOFF.md`.
13. Recorded the Codex review and reran final repository checks.

## Workspace Recovery Result

After Claude completed, discovery ranked
`run-20260628052600-7ikdkf` first for the exact normalized workspace and
reported status `awaiting_review`, last task
`task-20260628052634-xugsyp`, and the original Claude session. Explicit
`ai_bridge_attach_workspace_run` and `ai_bridge_poll_workspace_run` returned
that same completed task with `finalizationPhase: complete`; neither call
created a run, task, iteration, or session.

The task completed before an in-flight disconnect could be observed.
Completed-task workspace recovery was validated instead.

## Run Explorer Result

All six Run Explorer interfaces returned successfully:

- `ai_bridge_list_runs`: listed the current exact-workspace run first and
  isolated older terminal runs.
- `ai_bridge_inspect_run`: returned run/task state, the latest 25 events,
  structured verification, usage, and log paths.
- `ai_bridge_tail_run`: returned events 0-99, `nextCursor: 100`, and
  `hasMore: true`.
- `ai_bridge_show_run_diff`: the summary identified only
  `docs/validation/v0.4.4-real-dogfood-retry.md` as created after preflight and
  reported no modified pre-existing files. Both bounded patch modes returned
  safely; the patch body was empty because the document was still untracked.
- `ai_bridge_show_verification`: returned three passed commands with bounded
  output.
- `ai_bridge_export_run`: created non-overwriting redacted exports:
  `C:\Users\xsjhxs\.ai-bridge\exports\run-20260628052600-7ikdkf.json`
  (86,971 bytes) and
  `C:\Users\xsjhxs\.ai-bridge\exports\run-20260628052600-7ikdkf.md`
  (9,819 bytes). A same-path re-export was correctly refused. After review,
  final exports were written to
  `C:\Users\xsjhxs\.ai-bridge\exports\run-20260628052600-7ikdkf-reviewed.json`
  (91,506 bytes) and
  `C:\Users\xsjhxs\.ai-bridge\exports\run-20260628052600-7ikdkf-reviewed.md`
  (9,832 bytes).

The transcript contained 2,219 summarized events. Partial assistant deltas were
very granular, but no corrupt transcript lines were reported.

## Verification Commands

AI Bridge recorded the following structured results:

- `git diff --check`: exit `0`, 165 ms.
- `git diff --cached --check`: exit `0`, 166 ms.
- `npm run check`: exit `0`, 1,250 ms.

After Codex completed the documentation, the same checks were rerun locally.
`npm run check` continued to validate the Node syntax entry points. No test,
integration, skill-validator, or plugin-validator command was required because
the repository change was documentation-only.

## Review Result

Codex found no product-code defect. Claude stayed within its one-file boundary
and intentionally left live-observation fields for Codex. Codex completed those
fields and corrected two stale illustrative tool names to the actual APIs used.

`ai_bridge_record_review` persisted iteration 1 with disposition `pass`, moved
the run to `passed`, and reported `nextIterationAllowed: false`.

## Files Changed

- `docs/validation/v0.4.4-real-dogfood-retry.md`: created by Claude and
  completed by Codex.
- `CONTEXT_HANDOFF.md`: updated by Codex with the validation result.

No plugin file changed, so the standing reinstall-after-plugin-update rule did
not apply. The user-owned `hatch-pet-runs/` files remained untouched.

## What Passed

- Fresh-thread AI Bridge tool exposure.
- First-call workspace discovery and clean creation of one new run.
- Real Claude Code asynchronous execution with exit code 0.
- Persisted completed-task discovery, attach, and workspace poll.
- Stable run, task, iteration, and Claude session identity across recovery.
- Baseline-aware separation of Claude's file from pre-existing user files.
- All six Run Explorer interfaces.
- JSON and Markdown export without overwriting repository files.
- Structured verification and final local documentation checks.
- Secret-redaction and bounded-output paths returned no warnings for this run.

## What Failed Or Was Not Verified

- No product behavior failed in this documentation-only gate.
- An in-flight MCP disconnect was not observed because Claude finished first.
- Full MCP client disconnect and automatic reconnect were not tested.
- Live push replay or retroactive stream replay was not tested.
- Multiple consecutive real Claude iterations were not tested.
- This run does not prove general or permanent plugin discovery behavior.
- It does not prove all real Claude API behavior, public marketplace
  publication, DeepSeek billing, or real monetary savings.

## Limitations

- This is one local Windows thread and one short documentation task.
- Completed-task recovery is weaker evidence than in-flight recovery.
- The Run Explorer patch body did not include the untracked document; the
  baseline-aware snapshot and later Git review supplied the file-level evidence.
- Usage summary was token-only: 86,697 input, 6,048 output, 423,552 cache-read,
  0 cache-creation tokens, cache hit rate `0.830089`. No pricing was supplied,
  so no real or hypothetical cost claim is made.
- Partial-message transcript granularity produced many small events.

## Follow-up Decision

Treat the fresh-thread exposure and one complete real dogfood loop as passed for
this environment. Continue normal dogfood with bounded tasks. Keep in-flight
disconnect/reconnect, live replay, and repeated fresh-thread exposure as
separate future validation targets; do not describe them as proven by this run.
