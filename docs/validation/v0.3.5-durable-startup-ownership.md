# AI Bridge v0.3.5 Durable Startup Ownership Validation

Date: 2026-06-26

## Starting Version Check

- Source package version at start: `0.3.4`
- Source `APP_VERSION` at start: `0.3.4`
- MCP server version source at start: `APP_VERSION`
- Source plugin manifest version at start: `0.3.4+codex.20260624120000`
- Currently visible installed personal plugin cache versions: `0.2.0+codex.20260623112432`, `0.2.1+codex.20260624120000`
- Skill cache version available to this Codex session: `0.2.1+codex.20260624120000`
- Git HEAD at start: `167668f6fd551d58955d534b78225a4cd5e801a4`
- Initial `git status --short`: only pre-existing untracked `hatch-pet-runs/silver-armor-girl/...`

The running plugin/skill cache is not the same version as the source tree under validation. This v0.3.5 run is therefore source-level implementation and fixture validation, not full installed-plugin dogfooding.

## Acceptance Matrix

| Requirement | Implementation Function | Test Name | Real Target Event Triggered | Expected State | Failure Signal |
| --- | --- | --- | --- | --- | --- |
| P0: adopt `worker_spawned` reservation when `reservation.workerPid` exists but `task.workerPid` is missing | `classifyRunningTaskOwnership()`, `adoptReservationWorker()`, `recoverStartReservations()` | `recovery adopts reservation worker when task worker fields were not landed` | Real worker process is spawned by `startClaudeIteration()`; launcher exits after reservation worker fields land and before task worker fields land | task remains `running`, worker fields are backfilled, reservation becomes `worker_adopted` or `worker_ready`, task later completes | task is marked `failed`, unknown process is killed, or worker fields are not adopted |
| P0: poll and recovery share startup ownership classification | `classifyRunningTaskOwnership()`, `recoverRunningTasks()`, `pollClaudeIteration()` | `poll preserves task_created startup reservation owned by live launcher` | A live launcher process owns a `task_created` reservation while another process polls | task stays `running`; poll reports startup ownership diagnostic instead of orphaning | poll finalizes/orphans the task or uses stale activeChildren-only heuristic |
| P1: launcher identity is not PID-only | `getLauncherIdentityStatus()`, `classifyRunningTaskOwnership()` | `launcher identity detects PID reuse mismatch` | Reservation PID exists but process start time/executable/command line do not match | classifier returns `launcher_mismatched` / non-owned startup state | live PID alone is treated as matched |
| P1: stdin error listener actually ran | worker spawn/stdin handlers in `startClaudeIteration()` | `injected stdin EPIPE after listener installation records evidence and exits cleanly` | Worker exits/closes stdin while a large prompt is written; a test-only stream `EPIPE` is injected after listener installation to avoid platform pipe-buffer timing | task records `stdinErrorObserved=true`, code is `EPIPE` or platform equivalent, single finalization, no worker residue | close event alone satisfies test, no listener evidence, multiple terminal conflicts, or worker residue |
| P1: fenced write claim matches actual guarantee | `writeJsonWithFence()`, lock docs/tests | `stale writer is rejected before rewriting after fence loss` | Old writer loses lock before attempting another fenced write | stale write is rejected before write; docs describe the limited guarantee | test claims to prove assert-to-rename atomicity without actually doing so |
| Documentation updated to v0.3.5 | `README.md`, `CONTEXT_HANDOFF.md`, this validation record | documentation consistency checks | Docs were reviewed after final local validation and the final validation-gap CI result was recorded | docs list v0.3.5, final local test count, covered scenarios, limits, SHA, and CI result | old v0.3.3/52/old CI claims remain as current |
| Final SHA CI passes on both platforms | GitHub Actions `test` workflow | final GitHub Actions run | Pushed final SHA triggers CI | ubuntu-latest and windows-latest jobs succeed and run check/test/integration | CI references old SHA or either platform fails |
| Pre-commit cleanup completed before commit/push | cleanup commands | process and lock cleanup checks | Recursive lock/process/temp checks run after tests and before commit | no new unexplained worker/Claude/lock/temp residue | commit/push occurs before cleanup checks or unexplained residue remains |

## Long Test Budget

- `npm test` includes a 100 rapid start/fast-exit fixture. On Windows it can take around 5 minutes.
- Command timeout threshold for local full test runs should be at least 20 minutes.
- If an outer timeout kills the test runner, active workers must be classified as either still-running test work, timeout-caused residue, or normal-exit residue before being treated as a leak.

## AI Bridge Flow Note

This run is Codex fallback/source-level implementation. No new AI Bridge Claude iteration is counted as full closed-loop dogfooding for v0.3.5.

## Historical Baseline Local Verification

Executed on Windows from `C:\Users\xsjhxs\Desktop\ai_bridge` on 2026-06-26:

| Command | Result |
| --- | --- |
| `npm.cmd run check` | passed |
| `npm.cmd test` | passed, 62/62 tests, duration 356.575s |
| `npm.cmd run test:integration` | passed, fake-Claude integration task completed |
| `git diff --check` | passed, with LF-to-CRLF checkout warnings only |
| `python C:\Users\xsjhxs\.codex\skills\.system\skill-creator\scripts\quick_validate.py skills\ai-bridge` | passed |
| `python C:\Users\xsjhxs\.codex\skills\.system\plugin-creator\scripts\validate_plugin.py .` | passed |

The focused regression checks for worker adoption, task-created startup polling, launcher PID reuse mismatch, stdin error listener evidence, and stale fenced writer rejection also passed before the full test run.

## Validation Gap Supplement

On 2026-06-27, a forward supplement from historical validated baseline `2c69843f3603fcbb50f0b630a50a9c3b44edcc6a` added:

- deadline-aware waiting for unverifiable launcher and worker identity across poll, recovery, and cancel;
- assertions that task, run, and final logs are not terminalized before the startup deadline;
- awaited cleanup for fixture bridge homes, repositories, fake Claude directories, tracked test processes, and stale lock files;
- precise stdin error listener wording that identifies the injected post-listener `EPIPE`.

The supplemented local suite passed 66/66 tests in 376.645s. The resulting validation-gap commit passed its own GitHub Actions run `28277243715`.

## Cleanup Check

- `Get-Process -Name node` after the full test sequence showed no node processes started in the last 30 minutes.
- CIM `Win32_Process.CommandLine` queries were denied by local Windows policy, so command-line process residue checks were limited to available `Get-Process` metadata and test assertions.
- A broad `%TEMP%` scan found historical and fixture-created ai-bridge `.lock` files, including stale lock test artifacts. They were not deleted automatically. The automated fixture scenarios that require no lock residue assert cleanup inside their isolated `AI_BRIDGE_HOME`.
- Pre-existing untracked `hatch-pet-runs/silver-armor-girl/...` files remained untouched and uncommitted.

## CI Status

Final validated version: `v0.3.5`

- Final validation-gap SHA: `2d260d58659483d5054ab762e2323a1fa5c0e526`
- GitHub Actions run: `28277243715`
- `test (ubuntu-latest)`: success
- `test (windows-latest)`: success
- Both jobs ran `npm run check`, `npm test`, and `npm run test:integration`.
- `npm test` passed 66/66 tests with 0 failed and 0 skipped.

Historical baseline SHA `2c69843f3603fcbb50f0b630a50a9c3b44edcc6a` passed run `28234599534` with 62/62 tests before the validation-gap supplement.

The stdin test provides listener evidence through an injected `EPIPE` after listener installation. It does not prove that every operating system naturally produces the same pipe error under all timings.

The final validation-gap SHA above is the last validated source commit, not a guarantee that future repository HEAD remains identical.
